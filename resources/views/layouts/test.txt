<!doctype html>
    <html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">

        <meta name="csrf-token" content="{{ csrf_token() }}">
        <title>{{ config('app.name', 'Laravel') }}</title>

        <link rel="dns-prefetch" href="//fonts.bunny.net">
        <link href="https://fonts.bunny.net/css?family=Nunito" rel="stylesheet">
        <script src="{{ asset('js/signature_pad.umd.min.js') }}"></script>

        <link rel="stylesheet" href="{{ asset('bootstrap/css/bootstrap.css') }}">
        <link rel="stylesheet" href="{{ asset('bootstrap-icons/bootstrap-icons.css') }}">

        <style>
            #watermarkBtn {
                position: fixed;
                bottom: 20px; /* distance from bottom */
                right: 20px;  /* distance from right */
                z-index: 9999; /* always on top */
                opacity: 0.3; /* semi-transparent */
                transition: all 0.3s ease;
                cursor: pointer;
               
            }

            #watermarkBtn img {
                width: 60px; /* adjust size */
                height: auto;
                 border-radius: 10px;
            }

            #watermarkBtn:hover {
                opacity: 1; /* fully visible on hover */
                transform: scale(1.1);
            }
            body {
                font-family: 'Nunito', sans-serif;
                background: #f8f9fa;
                margin: 0;
            }
            #app {
                display: flex;
                flex-direction: column;
                min-height: 100vh;
            }
            .navbar {
                background: #2b4b7b !important;
                /* border-bottom: 1px solid #dee2e6;
                box-shadow: 0 1px 4px rgba(0,0,0,0.05); */
            }
            .navbar .nav-link.active {
                color: #0d6efd;
                font-weight: 600;
            }
            .navbar-brand {
                color: #ffffff !important;
            }
            .sidebar {
                width: 220px;
                position: fixed;
                top: 56px;
                left: 0;
                bottom: 0;
                background: #2b4b7b;
                /* border-right: 1px solid #dee2e6; */
                overflow-y: auto;
                padding-top: 1rem;
                transition: transform 0.3s ease;
            }
            .sidebar .nav-link {
                color: #ffffff;
                margin: 0.25rem 0.75rem;
                border-radius: 8px;
                display: flex;
                align-items: center;
                gap: 10px;
                transition: all 0.2s;
                width: 100%;
            }
            .sidebar .nav-link:hover {
                background: #e9f2ff;
                color: #2b4b7b;
            }
            .sidebar .nav-link.active {
                background: #ffffff;
                color: #2b4b7b;
            }
            .content-wrapper {
                margin-left: 220px;
                padding: 1rem;
                transition: margin-left 0.3s ease;
            }
            .content-full {
                margin-left: 0 !important;
            }
            .sidebar-collapsed {
                transform: translateX(-100%);
            }
            .card {
                border-radius: 10px;
                box-shadow: 0 3px 10px rgba(0,0,0,0.05);
            }
            .btn-outline-secondary {
                border-radius: 10px;
                padding: 6px 12px;
                border: 1px solid #2b4b7b;   /* Blue border */
                background: #ffffff;
                color: #2b4b7b;
                transition: all 0.25s ease;
                display: flex;
                align-items: center;
                gap: 5px;
            }
            
            .btn {
                color: #fff;
                background-color: #2b4b7b;
                border-color: #2b4b7b;
                transition: all 0.2s ease;
            }

            .btn:hover {
                color: #fff;
                background-color: #416498;
                border-color: #416498;
            }

            .btn:active,
            .btn:active:focus {
                color: #fff;
                background-color: #1e3557; /* darker when pressed */
             

            }

            .btn-outline-secondary:hover {
                background: #2b4b7b;
                color: #ffffff;
                border: 1px solid #fff;   /* Blue border */
                transform: translateY(-1px);
            }

            @media (max-width: 768px) {
                .sidebar {
                    transform: translateX(-100%); /* Hidden by default */
                    z-index: 9999;
                    position: fixed;
                }
                .sidebar.sidebar-open {
                    transform: translateX(0); /* Visible when opened */
                }
                .content-wrapper {
                    margin-left: 0 !important;
                }
            }

        </style>
        
    </head>
    <body class="@auth has-sidebar @endauth">
        <div id="app">

            <!-- Navbar -->
            <nav class="navbar navbar-expand-md navbar-light sticky-top">
                <div class="container-fluid">
                    @auth
                    <button class="btn btn-outline-secondary me-2" id="sidebarToggle">
                        <i class="bi bi-list"></i>
                    </button>
                    @endauth

                    @if((Auth::user()))
                    <a class="navbar-brand" href="{{ url('/') }}">
                        <i class="bi bi-layers"></i> {{ config('app.name', 'Laravel') }}
                    </a>
                    @endif

                    <ul class="navbar-nav ms-auto align-items-center">
                        @guest
                        @if(Route::has('login'))
                        <li class="nav-item">
                            <a class="nav-link {{ request()->routeIs('login') ? 'active' : '' }}" style="color: #fff;" href="{{ route('login') }}">
                                <i class="bi bi-box-arrow-in-right"></i> Login
                            </a>
                        </li>
                        @endif
                        {{-- @if(Route::has('register'))
                        <li class="nav-item">
                            <a class="nav-link {{ request()->routeIs('register') ? 'active' : '' }}" href="{{ route('register') }}">
                                <i class="bi bi-person-plus"></i> Register
                            </a>
                        </li>
                        @endif --}}
                        
                        @else
                        <li class="nav-item dropdown">
                            <a id="navbarDropdown" class="nav-link dropdown-toggle d-flex align-items-center gap-2" style="color: #fff;" href="#" role="button" data-bs-toggle="dropdown">
                                <i class="bi bi-person-circle fs-5"></i> {{ Auth::user()->name }}
                            </a>
                            <div class="dropdown-menu dropdown-menu-end shadow rounded-3 border-0 mt-1">
                                {{-- <a class="dropdown-item py-2" href="#"><i class="bi bi-gear"></i> Settings</a> --}}
                                {{-- <div class="dropdown-divider"></div> --}}
                                <a class="dropdown-item text-danger py-2" href="{{ route('logout') }}"
                                onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
                                <i class="bi bi-box-arrow-right"></i> Logout
                                </a>
                                <form id="logout-form" action="{{ route('logout') }}" method="POST" class="d-none">@csrf</form>
                            </div>
                        </li>
                        @endguest
                    </ul>
                </div>
            </nav>

            <!-- Sidebar -->
            @auth
            <div class="sidebar" id="sidebar">
                <ul class="nav flex-column">

                    {{-- Dashboard - visible to all --}}
                    <li class="nav-item">
                        <a class="nav-link {{ request()->is('admin') ? 'active' : '' }}" href="{{ url('/admin') }}">
                            <i class="bi bi-speedometer2"></i> Dashboard
                        </a>
                    </li>

                    {{-- Add Cheque - for admin and check-adder --}}
                    @if(in_array(Auth::user()->user_type, ['admin', 'check-add']))
                    <li class="nav-item">
                        <a class="nav-link {{ request()->is('add') ? 'active' : '' }}" href="{{ url('/add') }}">
                            <i class="bi bi-plus-square"></i> Add Cheque
                        </a>
                    </li>
                    @endif

                    {{-- Cheque Added - for admin and check-releaser --}}
                    @if(in_array(Auth::user()->user_type, ['admin', 'check-release']))
                    <li class="nav-item">
                        <a class="nav-link {{ request()->is('added') ? 'active' : '' }}" href="{{ url('/added') }}">
                            <i class="bi bi-card-checklist"></i> Cheque Added
                        </a>
                    </li>
                    @endif

                    {{-- Generate Report - for admin, check-releaser, and check-adder --}}
                    @if(in_array(Auth::user()->user_type, ['admin', 'check-release', 'check-add']))
                    <li class="nav-item">
                        <a class="nav-link {{ request()->is('checkadded/print') ? 'active' : '' }}" href="{{ url('/checkadded/print') }}">
                            <i class="bi bi-file-earmark-text"></i> Generate Report
                        </a>
                    </li>
                    @endif


                    {{-- Admin Settings - for admin only --}}
                    @if(Auth::user()->user_type === 'admin')
                    <li class="nav-item">
                        <a class="nav-link d-flex justify-content-between align-items-center {{ request()->is('settings*') ? 'active' : '' }}" 
                            data-bs-toggle="collapse" 
                            href="#adminSettingsMenu" 
                            role="button" 
                            aria-expanded="{{ request()->is('settings*') ? 'true' : 'false' }}" 
                            aria-controls="adminSettingsMenu">
                            <span><i class="bi bi-gear"></i> Admin Settings</span>
                            <i class="bi bi-chevron-down small"></i>
                        </a>

                        <div class="collapse {{ request()->is('settings*') ? 'show' : '' }}" id="adminSettingsMenu">
                            <ul class="nav flex-column ms-3 border-start ps-2">
                                <li class="nav-item">
                                    <a class="nav-link {{ request()->is('settings/users') ? 'active' : '' }}" href="{{ url('settings/users') }}" style="width: 90%;">
                                        <i class="bi bi-person"></i> Manage Users
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </li>
                    @endif

                </ul>
            </div>
            @endauth


            <!-- Page Content -->
            <main class="content-wrapper @guest content-full @endguest" id="contentWrapper">
                @yield('content')
            </main>
        </div>

        <a href="https://github.com/kobeRaf" target="_blank" id="watermarkBtn">
            <img src="/icon/KOBEICON.png" alt="GitHub" />
        </a>

        <script src="{{ asset('bootstrap/js/bootstrap.bundle.js') }}"></script>
        
        <script>

            document.addEventListener("DOMContentLoaded", function() {
                const toggleBtn = document.getElementById("sidebarToggle");
                const sidebar = document.getElementById("sidebar");
                const contentWrapper = document.getElementById("contentWrapper");

                if (toggleBtn && sidebar) {
                    toggleBtn.addEventListener("click", function() {

                        // Desktop toggle
                        if (window.innerWidth > 768) {
                            sidebar.classList.toggle("sidebar-collapsed");
                            contentWrapper.classList.toggle("content-full");
                        } 

                        // Mobile toggle
                        else {
                            sidebar.classList.toggle("sidebar-open");
                        }

                    });
                }
            });
        </script>
    </body>
</html>
